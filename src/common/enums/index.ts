export enum RouterType {
  Music = 0,
  Me = 1,
  MusicDetai = 2
}

export enum HttpStatusType {
  SUCCESS = 200,
  CLIENT_ERROR = 400,
  AUTHENTICATE = 401,
  FORBIDDEN = 403,
  NOT_FOUND = 404,
  SERVER_ERROR = 500,
  BAD_GATEWAY = 502,
  SERVICE_UNAVAILABLE = 503,
  GATEWAY_TIMEOUT = 504
}